// This root layout is intentionally minimal.
// The [locale] layout handles all page rendering with next-intl.
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
